# agents-cli version locks and configuration
# This ensures consistent CLI versions when working on this repo

agents:
  claude: latest
  codex: latest
  gemini: latest
  cursor: latest
  opencode: latest

dependencies: {}

mcp: {}

defaults:
  method: symlink
  scope: global
  agents:
    - claude
    - codex
    - gemini
    - cursor
    - opencode
